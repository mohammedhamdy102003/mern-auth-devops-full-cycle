apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: mern-devops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: mohammed102003/mern-frontend:latest
          ports:
            - containerPort: 80

